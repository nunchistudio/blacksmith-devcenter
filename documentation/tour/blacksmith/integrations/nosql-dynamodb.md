---

integrations:
  values:
    connection: "<table>"
    params:
      region:
        - "<region>"
      partition_key:
        - "<key>"
      sort_key:
        - "<key>"

---

Open the .env files to see how AWS credentials are set in the application.
